# ScoutLens — Data Quality Report v0 (SLS-006)

Automated structural validation, generated by
[`src/scoutlens/data/validation.py`](../src/scoutlens/data/validation.py).
Reproduce with:

```
uv run python -m scoutlens.data.validation
```

Scope: primary-key uniqueness, required-field missingness, row counts, and
the specific sentinel-value patterns found during SLS-005 schema profiling
(see [`data-dictionary.md`](data-dictionary.md)). Cross-table foreign-key
integrity (event→match, event→player, event→team, match→competition) is
SLS-007, not this report.

**Result: 29 ok, 5 warn, 0 fail (of 34 checks).** No structural blocker for
Gate 1. All 5 warnings were already known from SLS-005 — this run confirms
nothing new broke and gives them a machine-checkable, re-runnable form
instead of a one-off manual read.

| Status | Table | Check | Detail |
|---|---|---|---|
| OK | competitions | row_count_min | 7 rows (>= 1) |
| OK | competitions | pk_unique[wyId] | 7/7 unique |
| OK | competitions | not_null[wyId] | no nulls |
| OK | competitions | not_null[name] | no nulls |
| OK | competitions | not_null[type] | no nulls |
| OK | teams | row_count_min | 142 rows (>= 1) |
| OK | teams | pk_unique[wyId] | 142/142 unique |
| OK | teams | not_null[wyId] | no nulls |
| OK | teams | not_null[name] | no nulls |
| OK | teams | not_null[type] | no nulls |
| OK | players | row_count_min | 3603 rows (>= 1) |
| OK | players | pk_unique[wyId] | 3603/3603 unique |
| OK | players | not_null[wyId] | no nulls |
| OK | players | not_null[role] | no nulls |
| OK | players | not_null[birthDate] | no nulls |
| WARN | players | sentinel_zero[weight] | 93 rows have weight == 0 — likely "unknown" sentinel, not a real measurement |
| WARN | players | sentinel_zero[height] | 77 rows have height == 0 — likely "unknown" sentinel, not a real measurement |
| WARN | players | dual_null_sentinel[foot] | 4 real nulls + 48 empty strings — two distinct "unknown" encodings in the same column |
| OK | matches | row_count_min | 1941 rows (>= 1) |
| OK | matches | pk_unique[wyId] | 1941/1941 unique |
| OK | matches | not_null[wyId] | no nulls |
| OK | matches | not_null[competitionId] | no nulls |
| OK | matches | not_null[dateutc] | no nulls |
| OK | matches | not_null[status] | no nulls |
| OK | events | row_count_min | 3,251,294 rows (>= 1) |
| OK | events | pk_unique[id] | 3,251,294/3,251,294 unique |
| OK | events | not_null[id] | no nulls |
| OK | events | not_null[matchId] | no nulls |
| OK | events | not_null[teamId] | no nulls |
| OK | events | not_null[eventId] | no nulls |
| WARN | events | coordinate_bounds[x] | 1/6,501,847 (0.000015%) outside [0,100] |
| WARN | events | coordinate_bounds[y] | 4/6,501,847 (0.000062%) outside [0,100] |
| OK | events | mapping_coverage[event_id] | all 10 used values are mapped |
| OK | events | mapping_coverage[tags] | all 57 used tag ids are mapped |

## Decisions deliberately deferred, not made here

Per this module's design (report, don't silently fix — see the docstring
in `validation.py`): the `weight`/`height`/`foot` sentinel warnings on
`players` are **not** normalized by this check suite. None of the feature
families planned for the spike (`docs/feature-definitions.md`, SLS-013)
are anthropometric, so this is not currently load-bearing — but it stays
visible here rather than being silently resolved, in case that changes.

## Test coverage

13 unit tests in
[`tests/data/test_validation.py`](../tests/data/test_validation.py) cover
every check function against small synthetic Polars frames — pass/fail/warn
behavior is verified independently of the real dataset, so the checks
themselves are trustworthy before being pointed at 3.25M real rows.
