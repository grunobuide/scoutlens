# configs/

Spike configuration (thresholds, paths, seeds) goes here as it's needed —
e.g. a future `spike.yaml` holding the eligible-minutes thresholds from
[`../docs/project-charter.md`](../docs/project-charter.md) once those stop
being exploratory and need to be shared across scripts/notebooks. Not
populated yet — nothing in the pipeline currently needs external config.
