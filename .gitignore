data/*
!data/README.md
artifacts/*
!artifacts/README.md
# Small (a few KB) regenerable result summaries -- versioned so a clone
# can inspect published numbers without re-running the pipeline; still
# regenerated fresh, not hand-edited, by their respective run_*.py script.
!artifacts/gate2_results.json
!artifacts/robustness_results.json
!artifacts/transfer_analysis_results.json
!artifacts/statsbomb_replication_results.json
!artifacts/shrinkage_experiment_results.json
*.parquet
# The complete showcase player set is reproducible but ~147 MB as JSON.
# Reviewable contract/index/manifest/research files remain tracked; the player
# pack is distributed separately as an immutable, content-addressed asset.
public/showcase/v1/players/
web/node_modules/
web/.next/
web/out/
web/coverage/
web/public/showcase/
web/*.tsbuildinfo
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.env

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
