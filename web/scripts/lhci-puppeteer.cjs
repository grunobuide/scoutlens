module.exports = async function prepareLighthousePage() {
  // Activates LHCI's shared Puppeteer browser so Lighthouse connects to the
  // Playwright-pinned Chromium instead of launching a second system Chrome.
};
