# artifacts/

Gitignored. Generated spike outputs (plots, exported tables, model/report
artifacts) land here locally as they're produced — nothing here is
committed. The reports that matter (data quality, feasibility) live as
version-controlled prose in [`../docs/`](../docs) instead.
